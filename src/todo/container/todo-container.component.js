import todoContainerController from './todo-container.controller.js';
import todoContainerView from './todo-container.html';

const todoContainer = {
  controller: todoContainerController,
  template: todoContainerView,
};

export default todoContainer;
